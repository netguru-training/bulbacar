.row
  .col-lg-12
    = simple_form_for(car, html: {class: 'form-horizontal' }) do |f|
      = f.input :brand
      = f.input :model
      = f.input :seats

      %p
        = f.button :submit
        = link_to 'Cancel', :back
